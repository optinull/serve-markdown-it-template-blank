/**
 * Default template configuration data. List all possible options, so it may
 * serve as a living reference & be accessible via `sermit gen-config`.
 *
 * @name SermitBlankTemplate~Config
 * @type {Sermit~Config}
 * @constant
 * @readonly
 */
const DEFAULT_CONFIG = {}

module.exports = DEFAULT_CONFIG
